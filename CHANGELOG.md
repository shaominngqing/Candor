# Changelog

本项目从 `0.13.0` 起记录面向使用者和分发者的版本变化。

## 0.13.1

- 新增原生 DMG 安装包，内含 Candor 和“应用程序”快捷方式。
- DMG 与 ZIP 同时发布，分别提供 Apple Silicon 和 Intel 架构版本。
- 打包过程自动挂载并检查 DMG 内容、应用架构、签名和 SHA-256。
- 标签流水线默认发布正式 GitHub Release，不再需要手动转换预发布状态。

## 0.13.0

- 将 Swift 包、模块、测试目标、可执行文件和 Bundle ID 全部统一为 Candor。
- 清理旧产品命名、重复图标、过时规划文档和未使用界面组件。
- 构建前重建应用包，避免旧文件混入新产物。
- 补充 `UserDefaults` Required Reason API 隐私声明。
- 在 CI 中校验 Bundle 元数据、隐私清单、CPU 架构、代码签名和 Release 校验和。
- 保留 Apple Silicon 与 Intel 双架构独立下载包。

## 0.12.3

- 建立可复用的磁盘空间账本和目录大小索引。
- 提供大文件与目录、分级清理建议和完整应用卸载。
- 所有删除操作统一移到废纸篓，并在执行前进行路径安全校验。
